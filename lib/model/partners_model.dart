import 'package:ddeelly/core/imports/imports.dart';

class PartnerModel {
  final String image;

  PartnerModel({required this.image});
}
final List<PartnerModel> partners = [
  PartnerModel(image: AppImages.al),
  PartnerModel(image: AppImages.bv),
  PartnerModel(image: AppImages.ea),
  PartnerModel(image: AppImages.fi),
  PartnerModel(image: AppImages.no),
  PartnerModel(image: AppImages.al),
];
